apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: facturacion
  labels:
    app: api-gateway
    tier: gateway
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
