{
  "name": "facturacion-client",
  "version": "1.0.0",
  "description": "Cliente Node.js para probar el API de facturación",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node index.js",
    "docker:build": "docker build -t facturacion-client:latest .",
    "docker:run": "docker run --rm --name facturacion-client -e API_URL=http://host.docker.internal:8080 facturacion-client:latest",
    "docker:run:network": "docker run --rm --name facturacion-client --network castor-network -e API_URL=http://api-gateway:8080 facturacion-client:latest",
    "lint": "echo 'No linter configured yet'",
    "clean": "rm -rf node_modules package-lock.json"
  },
  "keywords": [
    "facturacion",
    "client",
    "testing",
    "api",
    "rest"
  ],
  "author": "Castor",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "colors": "^1.4.0"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/castor/facturacion-system"
  }
}
